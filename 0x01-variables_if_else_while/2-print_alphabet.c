#include <stdio.h>

/**
 * main - Entry point for program
 * Description: Print all letters of the alphabet in lowercase
 * Return: 0
 */

int main(void)
{	char letterrs = 'a';

	while (letters <= 'z')
	{	putchar(letters);
		letters = letters + 1;
	}

	putchar('\n');
	return (0);

}
